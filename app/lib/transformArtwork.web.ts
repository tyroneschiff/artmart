export async function transformArtwork(_localImageUri: string): Promise<{ transformedUrl: string; description: string; prompt: string }> {
  throw new Error('AI transformation requires the mobile app.')
}
